# Provider presets for QGrapho
#
# Copy a block into ~/.qgrapho/config.toml or run:
#   qgrapho provider add deepseek
#   qgrapho provider add moonshot
#   qgrapho provider add grok
#
# Model IDs change over time — run: qgrapho model list --provider <id>
# to fetch live models from each provider's /v1/models endpoint.
